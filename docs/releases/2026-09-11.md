## 2026-09-11 11:00 · main 全量 + 轮换 OSS Key

- **操作者**：Agent
- **方式**：`deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1`）；随后补回易支付环境变量并重启 API
- **范围**：frontend / admin / api / env
- **变更摘要**：
  - 发布当前 `main`（开源文档与 MIT README），**未**带上 `feat/kepu-optimize` 的 TokenFree 改动
  - 线上 OSS AccessKey 按本机 `deploy/.env.prod` 轮换
  - 全量覆盖 `.env` 时本机未带易支付项，已从本机密钥补回 `EPAY_PID` / `EPAY_KEY`
- **验证**：
  - [x] 远端 `http://127.0.0.1:8000/api/health` → `ok:true`，task_runtime healthy
  - [x] `https://www.printfilm.com/` → 200
  - [x] `https://www.printfilm.com/api/health` → `ok:true`
  - [x] `https://admin.printfilm.com/` → 200
  - [x] `ai-movie-api` active；worker 保持停用
- **备注 / 回滚**：回滚至上一包后 `systemctl restart ai-movie-api`。OSS Key 已轮换，回滚代码后仍应使用新 Key。
- **提交**：`9b2f756`
