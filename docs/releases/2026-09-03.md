# 发布记录 · 2026-09-03

## 2026-09-03 · 分镜视频双结算防护；杭州全量

- **操作者**：Agent
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1`）
- **范围**：api（含进程内 poller）/ admin 统计聚合
- **变更摘要**：
  - 分镜视频轮询收尾：行锁认领 `finalizing`，避免下载窗口内并发双记用量/双退预扣
  - `settle_task`：已有 unfreeze/settle 流水则禁止二次退款；锁用户/任务时 `populate_existing`
  - Seedance 用量：同 `task_run + billing_key` 幂等，不重复记账
  - 管理端项目扣费：按任务实扣 + 去重用量 / 孤儿用量汇总（展示口径）
- **验证**：
  - [x] 脚本 `DONE`（约 1.2 min）；`ai-movie-api` active；`/api/health` 200；poller running
  - [x] 前台 / admin 静态首页 200（`Last-Modified: Thu, 03 Sep 2026 08:34/08:35 GMT`）
- **备注**：旧双结算账务不调账；仅防新并发。用户 401 曾临时 clawback 已回滚。
  后续发布验收 Host 以 **https://www.printfilm.com/** 为准（kepu 仅保留）。

## 2026-09-03 · 主站切 www.printfilm.com（文档 / 发布脚本）

- **操作者**：Agent
- **方式**：更新 `deploy_kepu_8136.py` 验收 Host + 热修线上 `PUBLIC_BASE_URL`；文档同步
- **范围**：deploy 脚本（gitignore）/ docs / `PUBLIC_BASE_URL`
- **变更摘要**：
  - 发布验收与 DONE 输出改为 `www.printfilm.com` / `admin.printfilm.com`
  - `PUBLIC_BASE_URL`、易支付回调主站固定 www；kepu / printtfilm 仅兼容保留
- **验证**：
  - [x] `Host: www.printfilm.com` `/api/health` 200；首页 200
  - [x] 线上 `PUBLIC_BASE_URL=https://www.printfilm.com`；`EPAY_NOTIFY_URL` 已是 www
  - [x] kepu Host 仍 200（保留）
