## 2026-09-14 19:15 · 漫剧每镜资产上限 + Kie 9 张（全量）

- **操作者**：Agent
- **方式**：`deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1` `SKIP_LOCAL_BUILD=1`）；发布前打回滚包
- **范围**：api（沿用已有 frontend / admin dist）
- **变更摘要**：
  - AI 分镜每镜最多挂 3 个出镜角色、2 件道具、1 个场景（合计 ≤6）
  - 按本镜真正出镜的人封顶，不把整场名单前 3 人套到每一镜
  - 提交 Seedance/Kie 时参考图去重并截到 9 张，避免重复写入 content/images
- **验证**：
  - [x] 远端 `http://127.0.0.1:8000/api/health` → `ok:true`，task_runtime healthy
  - [x] `https://www.printfilm.com/api/health` → `ok:true`
  - [x] `https://www.printfilm.com/` → 200
  - [x] `https://admin.printfilm.com/` → 200
  - [x] 远端已含 `fragment_asset_limit.py` / `kie_limits.py`
  - [x] `ai-movie-api` active；worker 保持停用
- **备注 / 回滚**：
  - 回滚包：`/opt/ai_movie_rollback_20260914-191448.tgz`（87M，不含 venv / 生成媒体）
  - 环境备份：`/opt/ai_movie_rollback_20260914-191448.env`
  - 第 612 集已有分镜不会自动改引用表；再生成视频会走 9 张截断，要引用表变干净需再跑 AI 分镜
  - 回滚命令（在服务器执行）：

```bash
systemctl stop ai-movie-api
mv /opt/ai_movie/backend/.venv /tmp/ai_movie_venv_keep
rm -rf /opt/ai_movie
tar -xzf /opt/ai_movie_rollback_20260914-191448.tgz -C /opt
mv /tmp/ai_movie_venv_keep /opt/ai_movie/backend/.venv
cp -a /opt/ai_movie_rollback_20260914-191448.env /opt/ai_movie/backend/.env
systemctl start ai-movie-api
```

## 2026-09-14 18:50 · 科普逐镜衔接 + 扫描修复（全量）

- **操作者**：Agent
- **方式**：`deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1`）；发布前打回滚包
- **范围**：frontend / admin / api
- **变更摘要**：
  - 科普出图/出视频改为按镜头串行：后镜出图参考上一镜静帧，出视频参考上一镜尾帧
  - 分镜表去掉横向滑动，按中间栏宽度自适应
  - 合入当日代码扫描修复：任务轮询分发、取消范围、易支付回调行锁、上游 429/5xx 韧性、科普取消传播等
- **验证**：
  - [x] 远端 `http://127.0.0.1:8000/api/health` → `ok:true`，task_runtime healthy
  - [x] `https://www.printfilm.com/` → 200
  - [x] `https://www.printfilm.com/api/health` → `ok:true`
  - [x] `https://admin.printfilm.com/` → 200
  - [x] `ai-movie-api` active；worker 保持停用
- **备注 / 回滚**：
  - 回滚包：`/opt/ai_movie_rollback_20260914-184905.tgz`（73M，不含 venv / 生成媒体）
  - 环境备份：`/opt/ai_movie_rollback_20260914-184905.env`
  - 回滚命令（在服务器执行）：

```bash
systemctl stop ai-movie-api
mv /opt/ai_movie/backend/.venv /tmp/ai_movie_venv_keep
rm -rf /opt/ai_movie
tar -xzf /opt/ai_movie_rollback_20260914-184905.tgz -C /opt
mv /tmp/ai_movie_venv_keep /opt/ai_movie/backend/.venv
cp -a /opt/ai_movie_rollback_20260914-184905.env /opt/ai_movie/backend/.env
systemctl start ai-movie-api
```
