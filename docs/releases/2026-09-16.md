## 2026-09-16 13:42 · 科普去掉配乐字幕选项 + 去掉假垫乐杂音（全量）

- **操作者**：Agent
- **方式**：`deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1`）；发布前打回滚包
- **范围**：frontend / api
- **变更摘要**：
  - 科普风格配置去掉「后期配乐」「字幕预设」；选题页提示改为确认配音与成片方式
  - 无真实配乐文件时不再生成正弦波垫乐（成片嗡嗡杂音来源）；`resolve_bgm_path` 返回空则跳过 BGM
- **验证**：
  - [x] 远端 `http://127.0.0.1:8000/api/health` → `ok:true`，task_runtime healthy
  - [x] 远端 `resolve_bgm_path('轻快专业')` → `None`
  - [x] `https://www.printfilm.com/` → 200（`index-crAN9n_c.js`，`Last-Modified: Wed, 16 Sep 2026 05:41:00 GMT`）
  - [x] 前台包内无「后期配乐」「字幕预设」
  - [x] `https://admin.printfilm.com/` → 200
  - [x] `ai-movie-api` active；worker 保持停用
- **备注 / 回滚**：
  - 回滚包：`/opt/ai_movie_rollback_20260916-134128.tgz`（89M，不含 venv / 生成媒体）
  - 环境备份：`/opt/ai_movie_rollback_20260916-134128.env`
  - 未改 nginx / 证书 / Postgres·Redis 容器
  - 已生成片子需再点一次合成，杂音才会去掉
  - 回滚命令（在服务器执行）：

```bash
systemctl stop ai-movie-api
mv /opt/ai_movie/backend/.venv /tmp/ai_movie_venv_keep
rm -rf /opt/ai_movie
tar -xzf /opt/ai_movie_rollback_20260916-134128.tgz -C /opt
mv /tmp/ai_movie_venv_keep /opt/ai_movie/backend/.venv
cp -a /opt/ai_movie_rollback_20260916-134128.env /opt/ai_movie/backend/.env
systemctl start ai-movie-api
```

## 2026-09-16 10:15 · 生图按 Kie sunburst 计费 + 首页获客标签（全量）

- **操作者**：Agent
- **方式**：`deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1`）；发布前打回滚包
- **范围**：frontend / api（admin 随包重建，无功能改动）
- **变更摘要**：
  - 生图改走 TokenFree Kie `gpt-image-2-5-sunburst`，按积分档计费：1K 6 积分 / 2K 10 积分 / 4K 16 积分（默认 3.5 分/积分 → 2K **35 分**），不再用 OpenAI $0.625（约 438 分）
  - Seedream / 裸 `gpt-image-2-5` 请求会 remap；按张预扣不再乘 1.2；注册赠金 500 分冻得住一张图
  - 生成侧 Pro/sunburst 把 3K/4K 钳成 2K，预扣与结算跟钳后清晰度
  - 首页「AI短视频」卡片标题旁加「获客短视频」标签
- **验证**：
  - [x] 远端 `http://127.0.0.1:8000/api/health` → `ok:true`，task_runtime healthy
  - [x] 远端 `charge_fen_official_image(size=2K)` → 10 积分 / **35 分**
  - [x] `https://www.printfilm.com/` → 200（`index-kWZIgNr9.js`，`Last-Modified: Wed, 16 Sep 2026 02:14:16 GMT`）；中文可见「获客短视频」
  - [x] `https://admin.printfilm.com/` → 200
  - [x] `ai-movie-api` active；worker 保持停用
- **备注 / 回滚**：
  - 回滚包：`/opt/ai_movie_rollback_20260916-101439.tgz`（88M，不含 venv / 生成媒体）
  - 环境备份：`/opt/ai_movie_rollback_20260916-101439.env`
  - 未改 nginx / 证书 / Postgres·Redis 容器
  - 本机直连公网 HTTPS 可能 reset，验收以服务器本机 curl 与浏览器为准
  - 回滚命令（在服务器执行）：

```bash
systemctl stop ai-movie-api
mv /opt/ai_movie/backend/.venv /tmp/ai_movie_venv_keep
rm -rf /opt/ai_movie
tar -xzf /opt/ai_movie_rollback_20260916-101439.tgz -C /opt
mv /tmp/ai_movie_venv_keep /opt/ai_movie/backend/.venv
cp -a /opt/ai_movie_rollback_20260916-101439.env /opt/ai_movie/backend/.env
systemctl start ai-movie-api
```
