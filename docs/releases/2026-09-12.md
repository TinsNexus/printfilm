## 2026-09-12 18:55 · 保存分镜新建镜 MissingGreenlet（热修）

- **操作者**：Agent
- **方式**：SSH 热修（改 `access.py` + `episodes.py` + `systemctl restart ai-movie-api`）
- **范围**：api
- **变更摘要**：
  - `POST /api/drama/episodes/{id}/fragments` 新建无 id 分镜后，不再 `asset_references.clear()` 触发 async lazy load
  - 改为 `replace_fragment_asset_refs`：已加载集合只删多余/只补缺失（避免 `uq_drama_frag_asset` 同键先插后删）；未加载才 SQL 清库；`load_episode_fragments` 加 `populate_existing`
- **验证**：
  - [x] 本机 `http://127.0.0.1:8000/api/health` → `ok:true`，task_runtime healthy
  - [x] 前台首页 200（`www.printfilm.com`）
  - [x] admin 首页 200（`admin.printfilm.com`）
  - [x] 远端 `py_compile` 通过；helper 含 `set_committed_value` / `populate_existing`
  - [ ] 前台分集 `#609` 再点保存（含第 8 镜无 id）应 200
- **备注 / 回滚**：
  - `cp -a /opt/ai_movie/backend/app/api/drama/episodes.py.bak-20260912-savefrags /opt/ai_movie/backend/app/api/drama/episodes.py && cp -a /opt/ai_movie/backend/app/services/drama/access.py.bak-20260912-savefrags /opt/ai_movie/backend/app/services/drama/access.py && systemctl restart ai-movie-api`

## 2026-09-12 16:21 · 分镜自动重切保护顺序 + 挂回 #609 成片（热修）

- **操作者**：Agent
- **方式**：SSH 热修（改 `seed.py` + `systemctl restart ai-movie-api` + 数据挂回）
- **范围**：api / 数据
- **变更摘要**：
  - 线上 `_episode_should_replace_fragments` 先判断成片/手改保护，再判断是否场记原文；避免进分镜页 `seed_from_script` 把电影感手写分镜整集覆盖
  - 项目 `#424` 分集 `#609`「被贬成仙美食图鉴」第 1 镜（`18274`）挂回旧成片 `18261`（`shot_18261_1789197049.mp4`）；原 `18274` 成片写入 `video_versions`
- **验证**：
  - [x] `https://www.printfilm.com/api/health` → `ok:true`，task_runtime healthy
  - [x] 前台首页 200（`www.printfilm.com`）
  - [x] admin 首页 200（`admin.printfilm.com`）
  - [x] 线上函数顺序：`_episode_has_protected_fragments` 在 `is_raw_screenplay_fragment` 之前
  - [x] `18274.video` 指向 OSS `shot_18261_1789197049.mp4`，HEAD 200
- **备注 / 回滚**：
  - 代码：`cp -a /opt/ai_movie/backend/app/services/drama/seed.py.bak-20260912 /opt/ai_movie/backend/app/services/drama/seed.py && systemctl restart ai-movie-api`
  - 成片：在分镜页把第 1 镜切回历史版本（热修时已归档）
