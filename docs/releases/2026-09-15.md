## 2026-09-15 10:45 · 图片结算按张价（热修）

- **操作者**：Agent
- **方式**：SSH 热修 `billing/pricing.py` / `usage.py` / `estimates.py`；`systemctl restart ai-movie-api`
- **范围**：api
- **变更摘要**：
  - TokenFree 生图 `/v1/responses` 常回 0 token、无 quota，结算原先用 `45000 × 8 元/百万 ≈ 36 分`
  - 预扣已按 gpt-image 张价（约 438 分 × 1.2 缓冲 = 526 分），两边差约 14 倍
  - 无上游 quota 时，gpt-image / Seedream 家族改为按张官方价结算；有 `quota`/`cost_fen` 仍优先上游
- **验证**：
  - [x] 远端 `http://127.0.0.1:8000/api/health` → `ok:true`，task_runtime healthy，`models.image=seedream-5.0`
  - [x] `ai-movie-api` active
  - [x] 远端 `charge_fen_for_usage`：空 usage / 45000 token → 438 分；`cost_fen=500` → 500 分
- **备注 / 回滚**：
  - 备份：`/opt/ai_movie_hotfix_image_billing_20260915-1044`
  - 新单预扣仍约 526 分，实扣约 438 分（+少量 LLM）；历史单不重算
  - 回滚命令（在服务器执行）：

```bash
systemctl stop ai-movie-api
cp -a /opt/ai_movie_hotfix_image_billing_20260915-1044/*.py /opt/ai_movie/backend/app/services/billing/
systemctl start ai-movie-api
```

## 2026-09-15 10:32 · 漫剧分镜换行/片头 + 配音兜底（全量）

- **操作者**：Agent
- **方式**：`deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1`）；发布前打回滚包
- **范围**：frontend / admin / api
- **变更摘要**：
  - 漫剧分镜：换行续写不再单独占 3s；过碎 `@duration` 会合并进 15s 预算；时长标签可点击切换
  - 整集片头/背景叠字只留在开幕镜，后续片段读取/保存/生成时去掉重复片头
  - 默认音频 `qwen-tts-2025-05-22`；TokenFree speech 失败仍回退 edge-tts，不再写静音
- **验证**：
  - [x] 远端 `http://127.0.0.1:8000/api/health` → `ok:true`，task_runtime healthy，`models.audio=qwen-tts-2025-05-22`
  - [x] `https://www.printfilm.com/api/health`（本机 443）→ `ok:true`
  - [x] `https://www.printfilm.com/` → 200（`index-C9-2A0gl.js`，`Last-Modified: Tue, 15 Sep 2026 02:31:07 GMT`）
  - [x] `https://admin.printfilm.com/` → 200（`index-CQFdqNyv.js`）
  - [x] `ai-movie-api` active；worker 保持停用
- **备注 / 回滚**：
  - 回滚包：`/opt/ai_movie_rollback_20260915-103130.tgz`（88M，不含 venv / 生成媒体）
  - 环境备份：`/opt/ai_movie_rollback_20260915-103130.env`
  - 未改 nginx / 证书 / Postgres·Redis 容器
  - 已打开的分集页请强刷后再看片头与时长；保存一次会把修正写回库
  - 回滚命令（在服务器执行）：

```bash
systemctl stop ai-movie-api
mv /opt/ai_movie/backend/.venv /tmp/ai_movie_venv_keep
rm -rf /opt/ai_movie
tar -xzf /opt/ai_movie_rollback_20260915-103130.tgz -C /opt
mv /tmp/ai_movie_venv_keep /opt/ai_movie/backend/.venv
cp -a /opt/ai_movie_rollback_20260915-103130.env /opt/ai_movie/backend/.env
systemctl start ai-movie-api
```

## 2026-09-15 10:22 · 默认音频改 qwen-tts（热修）

- **操作者**：Agent
- **方式**：SSH 热修 `tokenfree_audio.py` / `ark.py`；生产 DB 渠道与默认音频改为 `qwen-tts-2025-05-22`；`systemctl restart ai-movie-api`
- **范围**：api
- **变更摘要**：
  - TokenFree 目录无 `seed-tts-2.0`，默认音频逻辑模型改为账号目录内的 `qwen-tts-2025-05-22`
  - `seed-tts*` 运行时会 remap 到 Qwen TTS；豆包 openspeech 的 `volc_tts_resource_id` 仍为 `seed-tts-2.0`
  - 实测 TokenFree `POST /v1/audio/speech` 对 qwen-tts 仍返回 500 `not implemented`；豆包 openspeech 仍 403；最终由 edge-tts 出声
- **验证**：
  - [x] `https://www.printfilm.com/api/health` → `ok:true`，`models.audio=qwen-tts-2025-05-22`
  - [x] 生产 `ArkGateway.tts()` 写出 `/static/generated/p0/shot_098_tts.mp3`（17KB，非近静音）
  - [x] 本地 `tests/test_tokenfree_audio.py` 通过
- **备注 / 回滚**：
  - 失败项目请再点「重新配音」；当前可听兜底仍是 edge-tts
  - 下次全量时 `deploy_kepu_8136.py` 的 `MODEL_AUDIO` 已改为 qwen，避免盖回 seed-tts

## 2026-09-15 10:15 · 科普配音 edge-tts 兜底（热修）

- **操作者**：Agent
- **方式**：SSH 热修 `ark.py` 后 `systemctl restart ai-movie-api`
- **范围**：api
- **变更摘要**：
  - 线上「整片配音近静音」：豆包 openspeech 403（资源未授权）且 TokenFree 无 `seed-tts-2.0` 渠道时，原先跳过 edge-tts 并写入静音
  - 现改为始终回退 edge-tts；全部失败则直接报错，不再写静音成片
- **验证**：
  - [x] `https://www.printfilm.com/api/health` → `ok:true`，task_runtime healthy
  - [x] `ai-movie-api` active
  - [x] 本地 `tests/test_tts_tokenfree_fallback.py` 通过
- **备注 / 回滚**：
  - 用户需对失败项目再点一次「重新配音」
  - 豆包 TTS 资源未授权、TokenFree 无语音模型属上游账号问题，edge-tts 仅为可听兜底

## 2026-09-15 10:07 · TokenFree 专一路由 + 分镜参考图上限（全量）

- **操作者**：Agent
- **方式**：`deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1`）；发布前打回滚包
- **范围**：frontend / admin / api
- **变更摘要**：
  - 开源版去掉 Kie 客户端与火山方舟管控面配置，图/视频/文本统一走 TokenFree
  - AI 分镜每镜最多 3 角色、2 道具、1 场景（合计 ≤6）
  - 视频参考图去重并截到 9 张（`media_ref_limits`）
  - 用户端失败文案改为 TokenFree 充值/网络提示
- **验证**：
  - [x] 远端 `http://127.0.0.1:8000/api/health` → `ok:true`，task_runtime healthy
  - [x] `https://www.printfilm.com/api/health` → `ok:true`，scheduler/poller/watchdog running
  - [x] `https://www.printfilm.com/` → 200（`index-D-q2ZcYB.js`，`Last-Modified: Tue, 15 Sep 2026 02:06:02 GMT`）
  - [x] `https://admin.printfilm.com/` → 200（`index-D_LP16nu.js`）
  - [x] `ai-movie-api` active；worker 保持停用
- **备注 / 回滚**：
  - 回滚包：`/opt/ai_movie_rollback_20260915-100631.tgz`（87M，不含 venv / 生成媒体）
  - 环境备份：`/opt/ai_movie_rollback_20260915-100631.env`
  - 未改 nginx / 证书 / Postgres·Redis 容器
  - 回滚命令（在服务器执行）：

```bash
systemctl stop ai-movie-api
mv /opt/ai_movie/backend/.venv /tmp/ai_movie_venv_keep
rm -rf /opt/ai_movie
tar -xzf /opt/ai_movie_rollback_20260915-100631.tgz -C /opt
mv /tmp/ai_movie_venv_keep /opt/ai_movie/backend/.venv
cp -a /opt/ai_movie_rollback_20260915-100631.env /opt/ai_movie/backend/.env
systemctl start ai-movie-api
```
