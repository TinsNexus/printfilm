# 全库代码扫描与修复报告

- 日期：2026-09-14
- 分支：`feat/kepu-optimize`
- 方式：8 个只读 agent 分域并行扫描（任务平台 / 科普流水线 / 上游客户端 / 计费认证 / 漫剧 / 基建 / 用户端前端 / 管理端前端），主会话逐条人工复核（排除误报）后分批修复，每批跑测试与静态检查。
- 验证：后端 `.venv/Scripts/python.exe -m pytest`；前端 `tsc -b --noEmit` + `oxlint`。

## 修改清单

| # | 级别 | 域 | 文件 | 问题 | 修改 | 验证 |
|---|------|----|------|------|------|------|
| 1 | CRITICAL | 任务平台 | `app/services/tasks/poller.py` | Selector 到期查询无 task_type 过滤，api/studio 的 deferred 视频被送进 drama 的 `poll_fragment_video_task`，因 fragment_id≤0 8 秒内必判"分镜已变更"失败，开放 API 文生视频实际不可用且永不计费 | 查询加 `task_type=="fragment_video"` 过滤；轻量任务由 ephemeral 轮询处理 | 新增 `tests/test_task_poller_dispatch.py` 3 测试 + 任务平台既有 35 测试通过 |
| 2 | HIGH | 任务平台 | `app/services/tasks/poller.py` | ephemeral 轮询每 8 秒对全部在途任务查上游（无 next_action_at 门控），放大上游压力 | 加 `next_action_at IS NULL OR <= now` 门控；循环体抽 `_poll_one_ephemeral_task`，单行异常隔离（CancelledError 透传） | 同上（门控测试） |
| 3 | HIGH | 漫剧 | `app/services/drama/jobs.py`、`app/api/drama/episodes.py` | `cancel_all_episode_video_jobs()` 无参全表取消：任一用户"取消全部"会把**所有用户**生成中的分镜置 cancelled；`_reset_fragment_video_generation` 同病 | 双 JOIN 到 DramaProject 按 user_id 过滤；签名改必传 `user_id`；API 调用点传 `user.id` | 新增 `tests/test_drama_cancel_scope.py`；漫剧相关 123 测试通过 |
| 4 | HIGH | 计费 | `app/api/billing.py` | 易支付回调读订单无行锁，并发重复 notify 可重复入账（钱包流水无唯一约束兜底） | 订单查询加 `with_for_update` 行锁，paid 状态二次复查走幂等返回 | `tests/test_security_hardening.py` 重复回调仅入账一次 |
| 5 | MEDIUM | 认证 | `app/api/v1/generation.py` | `GET /v1/tasks/{task_id}` 不校验归属，任意用户凭上游 task_id 可拖走他人视频 | 先查本地 TaskRun 归属（requested_by），无记录 404，再代理上游 | IDOR 拒绝/属主放行 2 测试 |
| 6 | MEDIUM | 认证 | `app/api/tools.py` | `GET /api/tools/tasks/{id}` 本地无 ToolRun 归属行时仍代理 `poll_video_task` 查上游，同 IDOR | 无本地行直接 404（image 路径本就有 user 过滤） | 404 测试 |
| 7 | LOW | 认证 | `app/services/epay.py` | 回调签名用 `==` 比较，非常量时间 | 改 `hmac.compare_digest` | 签名正反用例 + 源码守卫 |
| 8 | LOW | 认证 | `app/deps.py` | 顶部 import 块与 `security` 整体重复一份；`int(sub)` 对畸形 JWT 抛 ValueError（500） | 去重复块；sub 解析包 try/except 返回 401 | 非数字 sub 返回 None 测试 |
| 9 | LOW | 认证 | `app/services/password_reset.py` | 重置令牌 GET+DELETE 非原子，并发可消费两次 | 改 Redis `GETDEL` 原子取删 | 既有 4 测试（FakeRedis 补 getdel）通过 |
| 10 | LOW | 计费 | `app/api/billing.py` | `_notify_success` 三段重复判断且后两段为死分支 | 合并为一次状态集合判断 | 重复回调用例覆盖 |
| 11 | 死代码 | 计费 | `app/services/billing/settlement.py`、`billing/__init__.py` | `settle_project`（旧项目维度结算，跨项目串退风险）全库无调用 | 删除函数及包导出 | 全量测试 |
| 12 | 死代码 | 计费 | `app/services/billing/ephemeral.py`、`billing/__init__.py` | `run_with_registered_handler` 全库无调用；删后连带未用 import | 删除函数、导出与 `execute_task_run` 导入 | 全量测试 |
| 13 | HIGH | 科普流水线 | `app/services/pipeline.py` | `run_pipeline` 捕获 PipelineCancelled 后吞异常不 re-raise，TaskRun 误判 succeeded 照常扣费，并破坏 asyncio 取消语义 | 取消分支末尾补 `raise` | `test_kepu_cancel_propagation.py` 含源码守卫 |
| 14 | HIGH | 科普流水线 | `app/services/pipeline.py` | 三个 regen（image/video/audio）在项目已 CANCELLED 后仍写回素材并把项目"复活"；但上游成本真实发生 | 写回前读项目状态，已取消则跳过写回/复活，**用量记账照常**后抛 PipelineCancelled；image/video 成功写回时清 `video_skip_reason` | 同文件 5 测试 |
| 15 | HIGH | 科普流水线 | `app/services/tasks/handlers.py` | 6 个 kepu handler 取消语义不统一 | 新增 `_await_kepu` 统一包装：PipelineCancelled 且 `cancel_requested` 时返回 `{"cancelled": True}` 退冻结，否则上抛 | SimpleNamespace 三态测试 |
| 16 | MEDIUM | 科普流水线 | `app/api/projects.py`（update_shot） | 改旁白后旧 `full_narration.mp3` 与各镜 audio_url 仍被使用；改 img_prompt 不清隐私跳过标记；本端点是唯一漏调 `_demote_after_edit` 的编辑口（DONE 不回退） | setattr 前比较旁白真实变化（audio_dirty），删整片音频并清各镜 audio_url；素材类编辑清 `video_skip_reason`；结尾统一 demote | `test_kepu_shot_edit_demote.py` 2 测试 |
| 17 | MEDIUM | 科普流水线 | `app/models.py`、`app/main.py`、`pipeline.py`、`kepu_stages.py` | 真人隐私拦截只在当轮内存生效，每轮轮询/重试都重新提交 Seedance（重复扣费、重复触违规） | 新增 `shots.video_skip_reason` 列（additive schema 补丁，已对 dev 库执行）；拦截分支持久化 `privacy` 标记；`shot_video_ready/skipped` 让阶段判定与计费预估把"已跳过"视为收敛，静图合成 | `test_kepu_privacy_skip.py` 3 测试 |
| 18 | MEDIUM | 科普流水线 | `app/services/pipeline.py` | `_synthesize_continuous_audio` 新合成后不复查，近静音 TTS 会产出无声成片 | 拷贝新文件后 `is_near_silent_audio` 复查，近静音抛错终止 | `test_kepu_continuous_audio_qc.py` |
| 19 | HIGH | 上游客户端 | `app/services/ark.py` | `poll_task`/`fetch_task_once` 单次 GET 收到 429/5xx 即判任务 failed（上游一次限流就误杀计费任务）；网络异常在同步端点直接 500 | 新增 `_is_transient_http_status`/`_retry_after_seconds`：429/5xx 与网络异常按 running 退避（尊重 Retry-After，上限 60s），长轮询由 deadline 收敛，400/404 等 4xx 保持终态 | `test_ark_poll_resilience.py` 30 测试 |
| 20 | MEDIUM | 上游客户端 | `app/services/ark.py` | `gen_and_wait_seedance_body` 一个大 try 包住"建单+等待"，隐私拦截/失败/超时等任意异常都用同一 body **重新建单**，产生第二个计费任务 | 提交与等待分层捕获：仅"参考音频下载失败"剥离 reference_audio 重提一次（audio_fallback 门闩），其余异常立即上抛 | 同文件 5 场景（wait/submit × 音频/非音频/重提上限） |
| 21 | HIGH | 上游客户端 | `app/services/model_settings.py` | `_refresh_overlay` 过滤空串：后台「清除密钥」写入的 `""` 被丢弃，`get_settings()` 回落 `.env` 旧密钥——界面显示已清除，请求仍带旧 Key | overlay 仅跳过 None，空串作为"显式清空"保留 | `test_model_settings_overlay.py` 4 测试（含 env 旧值对照） |
| 22 | 死代码 | 上游客户端 | `app/services/ark_control_usage.py`、`tests/test_ark_control_usage.py` | 火山管控面 GetInferenceUsage HMAC 客户端（204 行）产品代码零引用，上游成本监控已改走 TokenFree（`services/admin/upstream_usage.py`） | 整模块连同专属测试删除（47 行） | 全量测试 |
| 23 | HIGH | 上游客户端 | `app/services/kie_client.py` | `get_job`/`get_veo` 单次 429/5xx/网络异常/非 JSON 即抛，wait 循环零容忍致生图/视频误失败收费；不校验 HTTP 200 包裹的业务错误码；success 无结果 URL 时返回 succeeded+空 URL，下载方必炸；重复的两个查询方法 | 新增 `KiePollTransientError`，抽公共 `_get_record`（瞬时/终态分类 + 业务 code 校验）；wait 循环容错退避到总超时；`fetch_video_once` 瞬时→running、终态 4xx→failed、success 无 URL→继续轮询 | `test_kie_poll_resilience.py` 21 测试 |
| 24 | 死代码 | 上游客户端 | `kie_client.py`、`kie_catalog.py` | `uses_kie`、`get_account_credits`、`default_kie_channel_models` 全库零调用；连带未用导入 | 删除三个函数并清 `get_media_model` 导入 | 全量测试 |
| 25 | 死代码 | 科普流水线 | `app/services/pipeline.py`、`app/services/ffmpeg_compose.py` | 去 worker 化后旧进程内入口与死阶段长期残留：`start_pipeline`、`_regen_tasks`、`_dispatch_side_task`、`dispatch_regen_image/video/audio/project_audio_and_compose/compose_only`、`_running` 字典、`_image_stage/_video_stage/_audio_stage`；ffmpeg 的 `concat_native_videos`、`_write_ass/_write_srt/_ass_ts/_escape_ass_text`；恒 False 的 `_use_native_video_audio` 死分支 | 全部删除（grep 双向验证零调用，任务平台 handler 驱动的 run_pipeline/regen_* 保留）；`cancel_pipeline` 简化为标记+恒 False（两处 API 调用不取返回值，行为等价）；native 分支删除后存活路径去缩进。pipeline 2145→1792 行，ffmpeg_compose 1596→1368 行 | 受影响 40 测试通过；全量回归无新增失败 |
| 26 | HIGH | 任务平台/漫剧 | `app/services/tasks/service.py`、`app/services/tasks/executor.py`、`app/services/drama/jobs.py` | 分镜视频轮询成功后存在下载落盘收尾窗口：用户恰在此时取消，`_mark_cancelled` 立即把冻结额**全额退款**，而收尾协程随后写入的 `usage_events` 永久 `settled=False`（悬空），钱货两失；并发认领也缺少收尾期防护（可能重复下载双记费） | 三层修复：① service 新增 `task_finalizing_window_open`/`clear_finalizing_window`（`payload.finalizing_until` 窗口键，naive 按 UTC、坏值/无键视为窗口外）；② poller 行锁认领后置 10 分钟 finalizing 窗口（`_FRAGMENT_FINALIZE_CLAIM_TTL`），`_mark_cancelled` 在窗口内让位（任务保持 cancel_requested，不结算不退款，由 poller 下轮与 TTL 兜底），认领后复查取消标志直接 fail 不下载；③ 下载落盘后行锁复查：已取消则新函数 `_settle_cancelled_after_finalized` 收敛为 cancelled 终态并 `settle_task` 按实多退少补（不激活后续顺序任务）；`BaseException` 释放认领时按"已落盘且取消→按实结算 / 已落盘→清标记唤醒 / 未落盘→回 polling"三分支处理 | 新增 `tests/test_finalizing_cancel_race.py` 15 测试（纯函数 7、mock 3、真实 freeze/settle 的 DB 集成 5：差额退款、窗口内冻结不动、过期全退、重复收敛幂等）；全量 511 passed / 9 基线失败 |
| 27 | MEDIUM | 测试基建 | `tests/conftest.py` | `db_session` 直连 `.env` 业务库 `printfilm`：① 仪表盘/财务等全局统计断言被真实数据污染（今日 calls 实际 50/102，断言 1）；② `create_all`/`ALTER TABLE` 等 DDL 自动提交直接作用于开发库；③ 缺 FK 行的测试靠业务库残留数据侥幸通过，掩盖真实缺陷；④ 增量列在 conftest 手写单列清单，与 `main._apply_schema_patches` 漂移（新库缺 `subtitle_preset` 等 10+ 列） | 新增 `_resolve_test_database_url`（业务库名派生 `<库名>_test`；已含 `test` 时原样使用，便于 CI 注入；库名字符白名单拒绝）与 `_ensure_test_database`（连 `postgres` 维护库 AUTOCOMMIT 检查并建库）；fixture 强制连独立测试库；增量列改为临时把 `app.main.engine` 指向测试 engine 后复用权威 `_apply_schema_patches`，finally 还原，杜绝补丁清单漂移 | 隔离后原 9 个基线失败中 4 个自动转绿；同时暴露并修复 1 个被脏数据掩盖的 FK 缺陷（见 #28）；全量 **520 passed / 0 failed** |
| 28 | 测试修复 | 测试 | `test_admin_stats.py`、`test_billing_integration.py`、`test_voice_design.py`、`test_voice_synthesis.py`、`test_seedance_billing_usage.py` | 扫描开始前即存在的 9 个基线失败 + 独立测试库暴露的 1 个隐藏失败：① 3 个 dashboard 用例写死全局计数（随 #27 数据隔离转绿，断言不改）；② `test_aggregate_usage_summary_breakdown` 插 `project_id=42/99/7` 无真实行违反 FK；③ `test_record_llm_chat_line_skips_without_billing_scope` 用 `select(UsageEvent)` 全表计数；④ voice_design 第二组 Settings 未显式清空凭证，被 `.env` 回填致判定 True；⑤ voice_synthesis 2 个用例仍断言旧短句，产品已改为 ≥2s 的新试听文案；⑥ seedance fallback 用例传 `project_id=9` 靠开发库残留行通过；⑦ upstream 快照插今日日期撞唯一约束（空库消失） | ② 建真实 Template+Project 并以实际 id 入参，不存在项目用 9999999；③ 计数加 `user_id` 过滤；④ 显式传三个空凭证；⑤ 断言更新为新文案（自我介绍+声线说明）并保留 8 字截断用例；⑥ 同样建真实项目行；①⑦ 无需改码 | 全量 **520 passed / 0 failed**（此前 496 passed / 9 failed） |
| 29 | MEDIUM | 任务平台/计费 | `app/services/tasks/executor.py`、`app/services/tasks/service.py`、`app/services/billing/settlement.py`、`app/services/tasks/scheduler.py` | ① `freeze_for_task` 仅捕获 ValueError：DB 瞬断等非 ValueError 裸逃出执行协程，任务永久卡 `leased` 只能等 watchdog 周期回收；② executor 结果分支有不可达死代码（`pending + deferred` 判断被上一联合分支先吞）；③ service 三个全库零调用死函数（`update_task_for_user`/`retry_task_for_user`/`count_awaiting_poll_tasks` + `TaskUpdateRequest` 导入）；④ 终态任务 billing_status 仍 `frozen` 无对账扫描（settle 瞬间进程被杀即冻结额不退、usage 悬空） | ① 抽 `_fail_task_before_start`（step 预读传入规避 populate_existing 卸关系；frozen 保留交 settle、否则归 none；记事件+settle+commit），ValueError 走 `insufficient_balance` 专用码、其他异常记异常类型码并 failed 收敛；② 删除不可达分支；③ 删三个死函数与未用导入（grep 双向验证）；④ settlement 新增 `reconcile_terminal_frozen_tasks`（只扫 succeeded/failed/cancelled 且 finished_at 超 120s 宽限的 frozen 行，逐行 settle_task 并吞单条异常；幂等门闩保证重复安全；finalizing 任务非终态不会被误伤），scheduler 孤儿扫描周期低频驱动 | executor 新增 2 测试（RuntimeError 收敛/余额不足专用码）；新增 `tests/test_billing_reconcile.py` 4 测试（超宽限按实结算、宽限内不动、非终态/已结算跳过、无用量全退）；全量 **526 passed / 0 failed** |
| 30 | HIGH（发版阻塞） | 用户端前端 | `frontend/src/pages/studio/StyleConfigPage.tsx`、`pages/drama/AssetsStep.tsx`、`pages/drama/episodeCanvas/EpisodeStoryboardPage.tsx`、`pages/drama/ProjectWorkspacePage.tsx` | `npm run build` 的 tsc 阶段 5 个错误直接阻断出包：① StyleConfigPage 守卫为 `if (!project && !error)`，项目初始加载失败时 error 非空穿透主页面，623 行 `project.shots` 非空访问白屏崩溃（真缺陷）；②③ 两处 `resolveDramaMediaUrl` 未用导入；④ ProjectWorkspacePage 路由改版后遗留死代码 `steps`/`goToStep`（及连带 `useMemo`/`buildProjectSteps`/`hasScript` 未用） | ① 守卫收紧为 `if (!project)`，error 态渲染 BillingErrorNotice，类型收窄后全页非空访问安全；②③ 删未用导入；④ 删死函数/死变量与连带导入，步骤导航已由 `applyLocationState` 路由态驱动（行为不变） | `npx tsc -b --noEmit` 零错误；`npm run build` 复验（见验证状态）；管理端 tsc/build 本就全绿 |

## 经评估暂不修改（需产品/架构决策，记录在案）

| 项 | 原因 |
|----|------|
| 登录接口无限流 / JWT 改密不可吊销 / API Key 与 JWT 权限等价无 scope | 影响面大的鉴权策略变更，需产品确认限流阈值与令牌体系设计 |
| 生产环境默认 secret_key / debug 无启动校验 | 部署通过环境变量注入；加硬失败可能阻断现有部署，拟与运维确认后加 warning |
| wallet_ledger 加 `(ref_type, ref_id)` 唯一约束 | 需先排查历史重复数据并做数据迁移，作为后续独立变更 |
| `config.py`/`schemas_settings.py` 的 `volc_*` 配置字段（4 字段 + 后台 overlay/clear 契约） | 唯一消费方 ark_control_usage 已删（#22），但管理端设置页表单可能仍渲染/提交；留待管理端前端批次连同 UI 一并删除，避免前后端不同步 |

## 当前验证状态

- 后端全量：**526 passed, 0 failed**（2026-09-14，#1–#30 完成后；扫描开始时为 496 passed / 9 failed）。
- 用户端 `frontend`：`npx tsc -b --noEmit` 零错误，`npm run build` 出包成功（#30）。
- 管理端 `admin`：`tsc` 零错误，`npm run build` 出包成功（本就全绿，未改动）。
- 数据库安全（#27）：`db_session` 强制 `<库名>_test` 独立测试库并自动建库（本地新增 `printfilm_test` 库），不再对开发库执行 DDL；增量列复用 `main._apply_schema_patches` 权威清单；CI 注入含 `test` 的 DATABASE_URL 即可适配。
- 后端语法/导入：改动模块均可正常 import；死代码符号 grep 零残留。

## 发版后待办（2026-09-14 发版时有意保留，按优先级排序）

1. **部署安全（建议尽快，改动各 1 行）**：`deploy/docker-compose.yml` Postgres/Redis 端口显式绑 `0.0.0.0`，公网主机应改 `127.0.0.1`；`main.py` CORS 在未配 `CORS_ORIGINS` 时回落 `["*"]` 且带凭证，生产默认应收敛。
2. **用户端运行时 HIGH**：EpisodeEditPage 10 分钟 while 轮询无 runToken、空 catch busy 死锁、duplicateFrag 拷贝产物、generateSelected 的 selectedIndex 漂移可能错扣费、useCanvasAutoSave inflight 丢编辑、CanvasPage key={id}、StoryboardPage 批量重配音改 regenAllAudio、EditorPage regen 后轮询。改后需逐页手测。
3. **管理端瑕疵**：OrdersPage `?trade=` 深链 effect 依赖与补拉、ProjectsPage 双 GET、WorksPage 审核确认、TemplatesPage premium 后端过滤、QueuesPage 防抖、client.ts JSON.parse try/catch、删 ArkVolcMediaPanel.tsx；并连同 #22 遗留的 `volc_*` 配置字段/表单一并清除。
4. **基建技术债**：main.py 三个 TIMESTAMP→TIMESTAMPTZ 迁移；on_event→lifespan；c000003 补丁断裂排查；补丁缺索引；oss ensure_browser_cors 启动阻塞；config 死字段、schemas ProgressEvent 死类；requirements 残留 celery==5.5.1 及 9 处引用；scripts/gen_template_covers.py 默认连库回退封面（加 --dry-run/--apply）；脚本 engine.dispose；gen_image_style_previews 扩展名错误。
5. **谨慎重构（测试护航）**：pipeline.py（1792 行）拆模块、reconcile N+1、_fail_task 拆分、前端 client/格式化函数去重。

（报告完）
